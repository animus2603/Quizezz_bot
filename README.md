# StudHub — Telegram Mini App (Quizizz + Маркетплейс)

## Что уже готово (Этап 0-5)

- Структура проекта: бот (aiogram3) + API (FastAPI) + Mini App (HTML/JS) в одном процессе.
- Категории маркетплейса теперь: **Все / Учебное / Товары** (услуги как отдельная
  категория убраны — учебные услуги и материалы объединены в «Учебное»).
- Ряд Все/Учебное/Товары + иконка фильтра — в одну строку, компактно.
- У категории «Учебное» есть примеры подкатегорий: Quizizz, СРС, Реферат, Доклад,
  Шпаргалки, Сканер, Курсовая работа, Дипломная работа, Презентация, Конспект,
  Лабораторная работа. У «Товары»: Учебники, Электроника, Одежда, Мебель, Канцелярия,
  Спортивные товары, Прочее. Списки заданы в `SUBCATEGORY_EXAMPLES` (`database/crud.py`)
  — можно редактировать без миграций.
- Фильтр в разделе «Категории» — универсальный: подстраивается под выбранную
  категорию. Для «Товары» академические поля (курс/группа/факультет/кафедра/предмет)
  скрываются, остаётся только «Тип» (подкатегория). Для «Учебное» и «Все» показаны все поля.
- Каждое поле фильтра открывает попап автокомплита (поиск сверху, список снизу).
  Для подкатегории список подмешивает готовые примеры даже при пустой БД
  (`GET /api/marketplace/filter-options/{field}?category=...&q=...`).
- В разделе «Разместить» — то же самое: после выбора Учебное/Товар показываются чипы
  с примерами подкатегорий (можно выбрать один клик или вписать свой вариант), а
  академические поля скрыты для категории «Товары».
- Раздел «Заказы»: переключатель сверху («Мои заказы» / «Мои объявления»). Карточка
  заказа — номер, название, предмет/курс/факультет/кафедра, цена, статус. Отмена
  заказа в течение часа после создания.
- Оплата: клиент сразу получает сообщение с реквизитами Kaspi; админ — уведомление
  с кнопками ✅/❌ сразу на первом сообщении. Отказ — с указанием причины клиенту.
- Mini App: 5 разделов внизу, однотонные SVG-иконки, единый градиент шапки/меню.
- Языковой попап: English, Русский, Қазақша, Türkmençe.
- Профиль: аватар из Telegram, привязка телефона с автообновлением.

## Чего пока нет (следующие этапы)

- Пункты меню профиля («Настройки», «Уведомления», «FAQ», «Поддержка») — заглушки.
- Миграции (Alembic) — при смене схемы модели проще пересоздать `db/app.db`.
- Автонапоминания админу о горящих дедлайнах custom-заказов.
- Постоянное хранение на Render требует Persistent Disk или внешнюю Postgres.

## ⚠️ Важно при обновлении на проде

Категория `services` заменена на `study`, добавлено поле `subcategory` у объявлений.
Если БД не пересоздаётся автоматически на каждом деплое — удалите старый файл
`db/app.db` после обновления кода, иначе приложение может упасть на несовпадении схемы.


## Изменения в разделе «Разместить» (Этап 0-6)

- Плитки категорий (Учебное/Товары) уменьшены — теперь такие же компактные пилюли,
  как в разделе «Категории».
- Все поля (Тип, Курс, Группа, Факультет, Кафедра, Предмет) теперь **только выбор**,
  без свободного ввода — открывают тот же попап автокомплита, что и в фильтрах
  «Категорий». Для каждого поля гарантированно доступен вариант «Прочее», даже если
  в базе ещё нет ни одного объявления (реализовано в `crud.search_listing_filter_options`).
- Поле «Контакт» стало нередактируемым (`readonly`) — подставляется автоматически
  из Telegram-профиля (`@username`, либо имя, если username не задан).
- После отправки объявления на модерацию клиент получает подтверждающее сообщение
  от бота (`notify_client_listing_submitted`).
- После решения администратора (✅/❌) сообщение с кнопками в админ-чате удаляется
  (или, если удалить нельзя, с него снимаются кнопки) — повторное нажатие невозможно.


## Изменения: каскадные поля, тайм-аут отказа, статусы (Этап 0-7)

- В разделе «Разместить» (Учебное) поля идут в порядке: **Факультет → Кафедра →
  Курс → Группа → Предмет**. Каждое следующее поле заблокировано, пока не выбрано
  предыдущее, и его варианты сужаются по уже выбранным значениям (кафедра показывает
  только те значения, что относятся к выбранному факультету, и т.д.) — реализовано
  через `CASCADE_ORDER` в `database/crud.py` и `api/routers/marketplace.py`
  (`GET /api/marketplace/filter-options/{field}?faculty=...&department=...&course=...`).
- Курс — максимум 4 (`COURSE_EXAMPLES` в `database/crud.py`).
- Кнопки подтвердить/отклонить у админа теперь исчезают сразу при нажатии
  (на сообщение нельзя нажать повторно) — и для заказов, и для объявлений.
- Отказ объявления: у админа есть 3 минуты написать причину — если не успел,
  объявление отклоняется автоматически, студент и админ получают уведомление
  (см. `_listing_reject_timeout` в `bot/handlers/admin.py`).
- После подтверждения индивидуального заказа клиенту приходит статус
  «Принято — будет готово в течение рабочего дня», а не общее «в работе». Как
  только админ завершает заказ через `/complete <id>`, клиенту приходит файл/ссылка.


## Изменения: заказы, профиль, поддержка (Этап 0-8)

- Исправлен баг: эндпоинт «Мои заказы» падал с ошибкой для готовых тестов
  (обращение к несуществующему полю `group_name` у каталога).
- Причина отказа заказа теперь сохраняется в БД (`Order.rejection_reason`,
  функция `crud.reject_order_with_reason`) и отображается клиенту в карточке заказа
  красным текстом — данные не привязаны к сообщениям в чате и не теряются, даже
  если студент удалит переписку с ботом.
- Карточка «Мои заказы» переписана по фиксированному шаблону: номер заказа,
  название, факультет, кафедра, курс, группа, цена, статус (и причина отказа,
  если есть) — каждое поле на отдельной строке.
- Исправлена привязка номера телефона: раньше подгрузка в Mini App стартовала
  только если `tg.requestContact()` возвращал `true`, а часть клиентов Telegram
  этого не гарантирует, хотя контакт всё равно уходит боту. Теперь опрос профиля
  запускается всегда после вызова `requestContact()`, а не только по колбэку.
- Кнопка «Поддержка» в профиле открывает чат с `@animus_sh1` через
  `tg.openTelegramLink()`.
- Профиль переработан: обложка-градиент с аватаром поверх, номер в виде
  компактной карточки, статистика («Заказов» / «Объявлений»).


## Изменения: профиль, каталог, FAQ/уведомления/настройки (Этап 0-9)

- Профиль: убрана кнопка «Привязать номер» — при первом открытии раздела номер
  запрашивается автоматически (через `tg.requestContact()`) и появляется сам,
  как имя и username; ручная кнопка-«обновить» осталась как резерв.
- Карточка готового теста переработана: порядок полей Факультет → Кафедра → Курс,
  плюс предмет как подзаголовок. Добавлены 2 кнопки — «Купить» и «Посмотреть»
  (открывает окно с примером 10-20 вопросов, поле `preview_text` в
  `QuizCatalogItem`, заполняется админом при добавлении теста в каталог).
- FAQ — полноценный раздел-аккордеон: список вопросов, каждый разворачивается/
  сворачивается по нажатию, 5 примеров вопросов на каждом из 4 языков.
- «Уведомления» — экран с переключателями (Заказы и оплата / Маркетплейс /
  Новости), сохраняются в `localStorage` браузера.
- «Настройки» — экран с выбором языка (открывает тот же попап языка) и ссылкой
  на поддержку.
- Исправлен баг: обращение к `item.group_name` у каталога тестов ошибочно было
  убрано в прошлый раз — на самом деле поле есть в модели, теперь снова
  корректно отображается в «Моих заказах».


## Изменения: отмена с возвратом, демо-товары, фиксы UI (Этап 0-10)

- Индивидуальные заказы теперь можно отменить в течение **3 часов** с момента
  создания — даже если заказ уже оплачен и принят в работу (не только пока
  ждёт оплаты). Если отменяется уже оплаченный заказ, админ получает
  сообщение с просьбой вернуть деньги вручную через Kaspi
  (`notify_admin_refund_needed` в `bot/notify.py`). Готовые тесты по-прежнему
  можно отменить только за 1 час и только пока не оплачены (файл уходит сразу
  после оплаты, отменять уже нечего).
- Кнопка «Посмотреть» на карточке теста теперь стилизована на одном уровне с
  «Купить» (акцентная обводка вместо серого фона).
- Исправлен баг: попап выбора языка не открывался из раздела «Настройки» —
  он был визуально скрыт позади подэкрана (конфликт z-index). Теперь язык
  выбирается из любого места приложения.
- В «Настройках» вместо дублирующей «Поддержки» — «Поделиться приложением»
  (открывает нативный диалог пересылки Telegram).
- Шапки подэкранов (FAQ / Уведомления / Настройки) визуально приведены к
  единому виду с главной шапкой приложения (одинаковые отступы и шрифт).
- В маркетплейс добавлены 3 демо-объявления в категории «Товары» (учебник,
  ноутбук, толстовка) — сразу видно, как выглядит лента, даже без реальных
  продавцов (см. `EXAMPLE_GOODS_LISTINGS` в `database/engine.py`).


## Изменения: экран деталей товара, комментарии, реферал, фото, цена (Этап 0-11)

- Карточки в разделе «Категории» стали кликабельными — открывают полноэкранную
  карточку товара: фото (карусель, если их несколько), название, цена, описание,
  продавец с кнопкой связи, комментарии (можно оставить свой) и блок «Похожие
  товары» снизу — всё уже было реализовано на бэкенде, в этом шаге дописан
  фронтенд (`openProductScreen`, `renderProductScreen`, `submitComment` в `app.js`).
- Демо-объявления есть в обеих категориях: «Товары» (учебник, ноутбук, толстовка)
  и «Учебное» (СРС, готовый Quizizz, реферат) — заполняются автоматически при
  первом запуске (`EXAMPLE_GOODS_LISTINGS` / `EXAMPLE_STUDY_LISTINGS` в
  `database/engine.py`). Так как это обычные строки в базе, удаление админом
  убирает карточку отовсюду разом — она не дублируется в разных местах.
- Отступ между кнопкой фильтра и списком карточек в «Категориях» увеличен.
- Смена языка в «Настройках» (и в FAQ / Уведомлениях) применяется сразу на
  месте, без выхода и повторного входа в раздел.
- Добавлена лёгкая реферальная программа: у каждого пользователя есть
  персональная ссылка-приглашение (`https://t.me/<bot>?start=ref_<tg_id>`),
  бот считает, сколько людей пришло по ссылке (`referred_by`, `referral_count`).
- Цена в разделе «Разместить» — только цифровая клавиатура, при вводе сама
  расставляются пробелы по разрядам (5000 → 5 000).
- Загрузка фото в «Разместить» — можно выбрать сразу несколько, снизу показываются
  миниатюры с кнopkой ✕ на каждой, чтобы убрать ненужное перед отправкой.


## Изменения: фото модерации, деталка заказов/объявлений, рейтинг, layout карточек (Этап 0-12)

- Исправлен баг: при отправке объявления на модерацию админу приходило только
  текстовое сообщение без фото, даже если студент прикрепил фотографии —
  функция проверяла старое поле `photo_file_id` (Telegram), а веб-загрузки
  хранятся в `photo_urls`. Теперь админ получает первое фото сразу в сообщении
  (`notify_admin_new_listing` в `bot/notify.py`), с пометкой количества фото,
  если их больше одного.
- «Мои объявления» стали кликabельными — открывают ту же полноэкранную карточку
  товара, что и в общей ленте (фото, описание, продавец, комментарии, похожие).
  Статус «На модерации» переименован в «Ожидание» — три статуса объявления
  теперь: Ожидание / Отклонено / Опубликовано.
- Комментарии под товаром: добавлен рейтинг 1-5 звёзд (можно поставить перед
  отправкой комментария, отображается рядом с именем автора). Кнопка отправки
  комментария — теперь аккуратная круглая иконка со стрелкой вместо текста,
  который не помещался в круглую кнопку и выглядел криво.
- Карточки объявлений (в общей ленте и в «Моих объявлениях») переведены на
  горизontальный layout: фото (или заглушка 📦, если фото нет) слева, название
  и цена — справа. Клик по карточке открывает полную карточку товара.


## Изменения: формат уведомления, фото на весь экран, кнопки, анимации, срок/редактирование (Этап 0-13)

- Сообщение админу о новом объявлении переформатировано: категория (человеко-
  читаемая — Учебное/Товары), название, описание, продавец **по имени** (не
  ссылка), контакт — уже готовая ссылка/юзернейм, по которой можно писать.
- Фото в карточке товара теперь открывается на весь экран по тапу — сверху
  справа кнопка ✕ для закрытия.
- Карточки объявлений в общей ленте получили явную кнопку «Посмотреть» перед
  «Написать продавцу» (тот же порядок, что уже был у карточек готовых тестов).
- В `style.css` добавлен общий набор transition/анимаций: плавное появление
  вкладок и карточек, лёгкое сжатие кнопок и карточек при нажатии, анимация
  открытия попапов/полноэкранного фото, всплытие тостов, анимация звёзд рейтинга.
- При создании объявления можно указать необязательный срок «Показывать до» —
  после этой даты объявление автоматически перестаёт попадать в ленту
  (фильтруется на уровне БД в `get_approved_listings`).
- «Мои объявления»: у каждой карточки — кнопки **Изменить** (открывает форму
  редактирования: название, описание, цена, контакт, срок) и **Удалить**
  (с подтверждением). Оба действия проверяют, что объявление реально
  принадлежит текущему пользователю (`PUT`/`DELETE /api/marketplace/listings/{id}`).

## ⚠️ Важно при обновлении на проде

Добавилось новое поле `expires_at` у объявлений. Если БД не пересоздаётся
автоматически на каждом деплое — удалите `db/app.db` после обновления кода.


## Изменения: отзывы, фото демо-данных, объединение ленты (Этап 0-14)

- Уведомление продавцу о новом отзыве (категория, название, имя автора без
  ссылки, сам текст) и редактирование/удаление своего отзыва через long-press
  (удержание пальцем/зажатие мыши на комментарии — 500мс) уже были готовы на
  бэкенде с прошлых шагов; в этом шаге дописан фронтенд: `bindOwnCommentLongPress`,
  `openCommentActionSheet`, `openEditCommentForm` в `app.js`.
- Исправлена причина, по которой фото в «Похожих товарах» выглядели пустыми:
  демо-объявления вообще не содержали фотографий. Добавлены реальные превью-фото
  (`picsum.photos`, стабильные по seed) для всех 6 демо-объявлений в
  `database/engine.py` — теперь заглушка 📦 показывается только для объявлений,
  где продавец реально не прикрепил фото.
- На главном экране под «Готовыми тестами» добавлен блок «Из маркетплейса» —
  показывает несколько последних объявлений (из «Категорий») с кнопкой
  «Смотреть все», которая переключает на вкладку «Категории». Карточки везде
  теперь используют одну общую функцию рендера (`renderMarketplaceCard`), так
  что раздел «Главная» и «Категории» выглядят единообразно.
- Кнопки «Посмотреть» и «Написать продавцу» на карточках получили иконки
  (глаз / конверт) для более аккуратного вида.


## Изменения: обязательные поля, срок без прошлых дат, реф.код, формат отзыва (Этап 0-15)

- В разделе «Разместить» все информационные поля стали обязательными: название,
  описание, цена, тип (подкатегория), а для «Учебное» — ещё и факультет/
  кафедра/курс/группа/предмет. Фото остаются по желанию (явно так помечены).
- Поле «Показывать до» теперь обязательно и не даёт выбрать прошедшую дату —
  ограничение стоит и в самом календаре (`min` на сегодня), и при отправке.
- В уведомление продавцу о новом отзыве добавлена строка «Тип: {подкатегория}»
  между категорией и названием товара/услуги.
- В профиле появился блок «Пригласи друга»: персональный код-номер, кнопки
  скопировать и поделиться (через нативную пересылку Telegram), счётчик
  приглашённых, и поле для ввода чужого кода (для тех, кто пришёл не по прямой
  ссылке) — `POST /api/users/apply-referral`.


## Изменения: обязательные поля, Главная-структура, до 10 фото, баллы за рефералов (Этап 0-16)

- «Разместить»: при незаполненных полях под кнопкой отправки появляется
  красный текст с тряской анимацией вместо обычного тоста — заметнее.
- Дата «Показывать до» не даёт выбрать прошедший день ни в календаре
  (атрибут `min`), ни при отправке (проверка в JS) — работает и в форме
  создания, и в форме редактирования объявления.
- Главная: у «Готовых тестов» и переименованного блока «Товары» (было «Из
  маркетплейса») теперь показываются только первые 7 карточек; кнопка
  «Смотреть все» появляется, только если элементов реально больше 7.
  Для тестов она открывает отдельный экран **только с тестами** (без
  маркетплейса), для товаров — переключает на вкладку «Категории».
- Исправлен баг: при 3+ фото в объявлении админ получал только одно. Теперь
  Telegram-боты не поддерживают кнопки внутри медиа-групп, поэтому все фото
  (**до 10 штук — это предел самого Telegram**) уходят единым альбомом, а
  следом отдельным сообщением — текст с кнопками подтверждения. Ограничение
  в 10 фото также стоит на самой форме загрузки на фронте.
- Long-press на своём отзыве теперь открывает компактное меню в том же стиле,
  что и попап выбора языка (а не полноэкранный лист, как раньше).
- Реферальная программа доработана: за каждого приглашённого друга (по ссылке
  или коду) пригласивший получает **10 баллов** (`User.points`,
  `crud.award_referral_points` / `apply_referral_code`). В профиле теперь
  показаны и код, и полная пригласительная ссылка отдельно, плюс текущий
  баланс баллов.

## ⚠️ Важно при обновлении на проде

Добавилось новое поле `points` у пользователя. Если БД не пересоздаётся
автоматически на каждом деплое — удалите `db/app.db` после обновления кода.

## Запуск локально

```bash
cd Quizezz_bot
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

cp .env.example .env
# заполните BOT_TOKEN (от @BotFather), ADMIN_CHAT_ID, KASPI_PHONE, KASPI_NAME

uvicorn api.main:app --reload --port 8000
```

Бот и API стартуют в одном процессе (бот поллингом — фоновой задачей внутри FastAPI).

## Чтобы открыть Mini App из Telegram

Telegram требует HTTPS-адрес для Web App. Для локальной разработки:

```bash
# в отдельном терминале
ngrok http 8000
```

Скопируйте выданный `https://...ngrok-free.app` в `.env`:
```
WEBAPP_URL=https://xxxx.ngrok-free.app/webapp/index.html
```
Перезапустите `uvicorn`, затем откройте бота в Telegram и нажмите /start.

## Как узнать ADMIN_CHAT_ID

Напишите боту `@userinfobot` — он покажет ваш `id`. Для группы админов —
добавьте бота в группу и используйте отрицательный ID группы.

## Структура проекта

```
Quizezz_bot/
├── api/                # FastAPI: REST для Mini App
│   ├── main.py
│   ├── schemas.py
│   └── routers/
│       ├── quiz.py
│       └── marketplace.py
├── bot/                # aiogram 3
│   ├── loader.py       # общий Bot/Dispatcher
│   ├── main.py         # регистрация роутеров, запуск polling
│   ├── notify.py       # уведомления админу
│   └── handlers/
│       ├── start.py
│       ├── payments.py # приём чеков от студентов
│       └── admin.py    # подтверждение оплат, модерация, /complete
├── database/           # SQLAlchemy модели + CRUD
├── webapp/             # Mini App (без сборки, чистый HTML/JS)
│   ├── index.html
│   ├── css/style.css
│   └── js/app.js
├── db/                 # тут появится app.db (SQLite)
├── config.py
├── requirements.txt
└── .env.example
```

## Деплой (когда дойдём)

Проще всего — VPS (Timeweb/Selectel/DO) + Nginx + systemd, или Railway/Render
для быстрого MVP-хостинга. Тогда `WEBAPP_URL` и `API_BASE_URL` укажут на
реальный домен вместо ngrok.
